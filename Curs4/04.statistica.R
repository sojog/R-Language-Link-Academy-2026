
x <- 1:10
cat("mean (average)", mean(x), "\n")

cat("median (cel din mijloc)", mean(x), "\n")

cat("min (minim)", min(x), "\n")
cat("max", max(x), "\n")

cat("summary (sumar)", summary(x), "\n")

cat("Inter Quartile range", IQR(x), "\n")

cat("Variance", var(x), "\n")
cat("Standard Deviation", sd(x), "\n")